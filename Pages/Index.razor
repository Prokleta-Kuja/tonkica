@attribute [Microsoft.AspNetCore.Components.RouteAttribute(C.Routes.Root)]

<div class="d-flex align-items-center justify-content-between mt-3">
    <h1>Hello, world!</h1>
    <QueryStepper QueryPart=@QUERY_YEAR DefaultValue=_defaultYear OnChange=ChangeYear />
</div>

@foreach (var issuer in _issuers.Where(i => _issuerDashboards.ContainsKey(i.Key)))
{
    <h4 class="mt-3">@issuer.Value.Name</h4>
    <div class="row">
    @if (_issuerDashboards.ContainsKey(issuer.Key))
        {
            var dashboard = _issuerDashboards[issuer.Key];
            <div class="col">
                <div class="card text-white bg-success h-100 mb-3">
                    <div class="card-header">
                        <h5 class="card-title">Total Income</h5>
                    </div>
                    <div class="card-body text-center p-0">
                        <table class="table table-sm table-borderless text-white mb-0">
                            <tbody>
                                @foreach (var category in dashboard.IncomeCategories)
                                {
                                    <tr>
                                        <td class="text-start">@category.Key</td>
                                        <td class="text-end">@C.Formats.AsDecimal(category.Value, 2)</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer text-end">
                        <h3 class="m-0">@C.Formats.AsDecimal(dashboard.Income, 2) @issuer.Value.Currency?.Tag</h3>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card text-white bg-danger h-100 mb-3">
                    <div class="card-header">
                        <h5 class="card-title">Total Expenses</h5>
                    </div>
                    <div class="card-body text-center p-0">
                        <table class="table table-sm table-borderless text-white mb-0">
                            <tbody>
                                @foreach (var category in dashboard.ExpenseCategories)
                                {
                                    <tr>
                                        <td class="text-start">@category.Key</td>
                                        <td class="text-end">@C.Formats.AsDecimal(category.Value, 2)</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer text-end">
                        <h3 class="m-0">@C.Formats.AsDecimal(dashboard.Expense, 2) @issuer.Value.Currency?.Tag</h3>
                    </div>
                </div>
            </div>
            <div class="col">
                <div class="card h-100 mb-3">
                    <div class="card-header">
                        <h5 class="card-title">TODO: estimation + limit + profit</h5>
                    </div>
                    <div class="card-body text-center p-0">
                        <table class="table table-sm table-borderless mb-0">
                            <tbody>
                                @foreach (var quarter in dashboard.Quarters)
                                {
                                    <tr>
                                        <td class="text-start">Qtr @quarter.Key</td>
                                        <td class="text-end">@C.Formats.AsDecimal(quarter.Value, 2)</td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                    <div class="card-footer text-end">
                        <h3 class="m-0">@C.Formats.AsDecimal(dashboard.Income + dashboard.Expense, 2) @issuer.Value.Currency?.Tag</h3>
                    </div>
                </div>
            </div>
        }
    </div>
}